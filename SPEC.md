# LockTodo — 잠금화면 투두 앱 스펙

> 작성일: 2026-06-07 · 상태: spike 착수 전 (핵심 가정 리서치 완료, 실측 게이트 대기)
> 플랫폼: Android (Kotlin 네이티브) · 타깃 검증: 에뮬레이터 `Medium_Phone_API_36.1` (Android 16)

## 1. 개요

잠금화면을 풀지 않고도 **할 일 목록을 보고 바로 체크**할 수 있는 안드로이드 앱입니다. 화면을 켜면 잠금화면 위에 할 일 목록이 떠서, 사용자가 폰을 들 때마다 할 일을 확인·완료할 수 있습니다. 기능은 **할 일 추가 / 삭제 / 체크** 세 가지로 한정합니다.

## 2. 목표 / 비목표

**목표 (딱 이것까지)**
- 할 일을 **추가**합니다 (텍스트만).
- 할 일을 **삭제**합니다.
- 할 일을 **체크**(완료 토글)합니다 — 잠금화면 위에서 잠금 해제 없이.
- 화면을 켜면 잠금화면 위에 할 일 목록이 자동으로 표시됩니다.

**비목표 (이번 범위 밖)**
- 날짜·기한·시계·우선순위·카테고리·정렬·반복 등 일체의 부가 속성
- 클라우드 동기화, 계정/로그인
- 위젯 방식 (탭 시 잠금 해제를 요구해 "무잠금 체크" 목표에 맞지 않아 제외)
- iOS

## 3. 핵심 가정과 리스크

리서치로 검증된 사실과, 코드로 증명해야 하는 게이트를 구분합니다.

### 검증된 사실 (문서·리서치 근거)
- 잠금화면 위젯은 탭하면 잠금 해제를 요구합니다 → "무잠금 체크"에 부적합. 그래서 **위젯이 아니라 `showWhenLocked` Activity 방식**을 택합니다.
- `setShowWhenLocked(true)` Activity는 잠금화면 위에 표시되며, 그 화면 안의 UI는 잠금 해제 없이 조작됩니다 (알람·전화 앱과 동일 메커니즘).
- Android 10+ 는 백그라운드에서 Activity 시작을 제한합니다. 단 **SYSTEM_ALERT_WINDOW(다른 앱 위에 그리기) 권한이 있으면 이 제한을 우회**할 수 있습니다. 사용자가 권한을 수동 허용하는 전제로 진행합니다.
- `ACTION_SCREEN_ON` 은 매니페스트 정적 등록이 안 되고 런타임 동적 등록만 됩니다 → 항상 받으려면 **포그라운드 서비스가 상주**해야 하며, 그 서비스는 알림을 하나 표시합니다(안드로이드 규칙).

### spike 게이트 (코드로 증명해야 끝나는 최대 리스크)
> **G1. 에뮬레이터 잠금 상태에서 화면을 켰을 때, 앱 화면이 자동으로 잠금화면 위에 뜨고, 잠금 해제 없이 체크박스 토글이 동작한다.**

G1 이 통과하면 프로젝트가 성립합니다. 실패하면 표시 방식(full-screen intent 등)을 재설계합니다. 본 개발은 G1 통과 후 시작합니다.

## 4. 아키텍처

```
[ForegroundService] 상주
   └ ACTION_SCREEN_ON 동적 수신
        └ 화면 ON → LockActivity 실행 (SYSTEM_ALERT_WINDOW 로 BAL 우회)

[LockActivity]  setShowWhenLocked(true) + setTurnScreenOn(true)
   └ 할 일 목록 + 체크박스 (잠금 해제 없이 토글)

[MainActivity]  앱 본체
   └ 권한 요청(SYSTEM_ALERT_WINDOW, POST_NOTIFICATIONS) + 서비스 시작 + 할 일 추가/삭제
```

- 저장: spike 단계는 인메모리/SharedPreferences, 본 개발은 Room.
- 할 일 모델은 `{ id, text, done }` 만. 부가 속성 없음.
- 체크 토글은 LockActivity 내부 상태 → 저장소 반영.

## 5. 화면 / 기능

| 화면 | 내용 |
|------|------|
| LockActivity (잠금화면 위) | 할 일 목록 + 각 항목 체크박스 (추가/삭제는 없음, 보기·체크만) |
| MainActivity (앱 본체) | 권한 안내·요청, 할 일 추가/삭제, 서비스 on/off |

## 6. 검증 전략

- **싼 단계**: `./gradlew assembleDebug` 빌드 통과 + 단위테스트(할 일 토글 로직, JVM).
- **실측 단계(가장 비쌈)**: 에뮬레이터에 설치 → `adb` 로 잠금 → 화면 ON → 자동 표시·무잠금 체크를 `adb logcat`·`adb exec-out screencap` 으로 관찰. chrome-devtools 가 아니라 **adb 가 실측 도구**입니다.
- **완료 판정(G1)**: 잠금 상태 화면 ON 시 LockActivity 표시 확인 + 체크 토글 로그 확인 + 스크린샷 확보.

## 7. 제출 요건

| 항목 | 필수 | 처리 |
|------|------|------|
| GitHub URL | ✅ | 첫 커밋부터 공개 레포로 운영 |
| 구현 과정 전부 커밋 | 선택 | 단계마다 커밋 (spike → 기능별). 커밋 author 에 claude 미포함 |
| README 원페이저 | 선택 | 루트 `README.md` 에 요약 (존댓말, 동사형) |
| Demo 스크린샷 | 선택 | 에뮬 잠금화면 표시·체크 장면을 `adb screencap` 으로 캡처해 README 에 첨부 |

## 8. 단계

1. **spike (G1 증명)**: 최소 앱 — ForegroundService + showWhenLocked LockActivity + 권한. 에뮬에서 G1 검증. = 골든 케이스.
2. **MVP (추가/삭제/체크)**: 할 일 추가·삭제(MainActivity) + 체크(LockActivity) + 저장 연동 + 토글 로직 단위테스트.
3. **마감**: 권한 온보딩, 재부팅 복원, 서비스 종료 엣지, README + 데모 스크린샷.
